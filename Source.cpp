#include"BattleShips.h"

int main() {
	Game tmp;

	tmp.Start_Game();

	//bool quite = false;
	//while (!quite) {
	//	if (con_keyPressed()) {
	//		if (con_getKey() == CON_KEY_ENTER) {
	//			quite = 1;
	//		}
	//	}
	//}
	//Drawer Drawww;
	////Drawww.is_pressed();
	//size_t tmp = GetRandomInt(0, 9);
	//while (true)
	//{
	//	tmp = GetRandomInt(0, 9);
	//	Drawww.is_pressed();
	//	std::cout << tmp << "\n";
	//}

	return 0;
}